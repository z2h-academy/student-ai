[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "text-utils"
version = "0.1.0"
description = "Utilidades de procesamiento de texto para pipelines de IA"
requires-python = ">=3.10"
dependencies = []
